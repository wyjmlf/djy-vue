/**
 * 字符串截取
 * @param {*} str 
 */
export var vue_filters={

    // strLength:function(message,leg){
    //     console.log(leg)
    //         if(message.length>leg){
    //             message=message.substring(leg);
    //         }
    //         return message;
    //     }
}

